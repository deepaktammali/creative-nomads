import {keyframes} from "styled-components";


const rotateFullKeyframe = keyframes`

    from{
        transform: rotate(0deg);
    }

    to{
        transform: rotate(360deg);
    }

`;


export {rotateFullKeyframe};